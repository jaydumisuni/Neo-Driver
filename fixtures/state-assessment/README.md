# Phase 9 state-assessment fixtures

Phase 9 is read-only. Machine state is never modified by these fixtures.

The executable proof for this phase is carried by the `neo-state-plan` regression suite and the `neo-state-assess` proof binary. Production operating-system targets are deliberately absent from this phase.
