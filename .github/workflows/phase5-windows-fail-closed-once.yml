name: Phase 5 Windows Fail Closed Once

on:
  push:
    branches: ["staging-phase5-driver-install"]

permissions:
  contents: write

jobs:
  windows-fail-closed-once:
    if: ${{ !contains(github.event.head_commit.message, '[phase5-windows-fail-closed]') }}
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: staging-phase5-driver-install
          fetch-depth: 0
      - name: Install stable Rust
        shell: pwsh
        run: |
          rustup toolchain install stable --profile minimal
          rustup component add rustfmt clippy --toolchain stable
          rustup default stable
      - name: Apply bounded fail-closed fixes
        shell: pwsh
        run: python tools/phase5_windows_fail_closed_fix.py
      - name: Prove Windows backend and regressions
        shell: pwsh
        run: |
          cargo fmt --all
          cargo check -p neo-driverstore --all-targets --locked
          cargo clippy -p neo-driverstore --all-targets --locked -- -D warnings
          cargo test -p neo-driverstore --locked
          git diff --check
      - name: Commit proven fixes and self-clean
        shell: pwsh
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add crates/neo-driverstore/src/windows.rs
          git rm .github/workflows/phase5-windows-fail-closed-once.yml tools/phase5_windows_fail_closed_fix.py
          git commit -m "fix: fail closed on Windows device status evidence [phase5-windows-fail-closed]"
          git push origin HEAD:staging-phase5-driver-install
