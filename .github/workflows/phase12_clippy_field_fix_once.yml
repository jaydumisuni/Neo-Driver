name: Phase 12 one-shot Clippy field correction

on:
  push:
    branches:
      - implementation/phase-12-mcp-rpc-tweak-authority

permissions:
  contents: write

jobs:
  correct:
    if: github.event.head_commit.message != 'Derive Phase 12 receipt mission from transaction'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Phase 12 branch
        uses: actions/checkout@v4
        with:
          ref: implementation/phase-12-mcp-rpc-tweak-authority
          fetch-depth: 0

      - name: Apply exact Clippy correction
        run: |
          python tools/phase12_clippy_mission_field_fix.py
          cargo fmt --all
          rm tools/phase12_clippy_mission_field_fix.py
          rm .github/workflows/phase12_clippy_field_fix_once.yml

      - name: Commit correction
        run: |
          git config user.name "THE TECH GUY"
          git config user.email "65334711+jaydumisuni@users.noreply.github.com"
          git add -A
          git commit -m "Derive Phase 12 receipt mission from transaction"
          git push origin HEAD:implementation/phase-12-mcp-rpc-tweak-authority
