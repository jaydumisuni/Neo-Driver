name: Phase 8 Linux Clippy Once

on:
  push:
    branches: [implementation/phase-8-runtime-executor]

permissions:
  contents: write

jobs:
  apply-and-prove:
    if: ${{ !contains(github.event.head_commit.message, '[phase8-linux-clippy-done]') }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: implementation/phase-8-runtime-executor
          fetch-depth: 0

      - name: Install stable Rust
        run: |
          rustup toolchain install stable --profile minimal
          rustup component add rustfmt clippy --toolchain stable
          rustup default stable

      - name: Apply cfg hygiene
        run: python tools/phase8_linux_clippy_once.py

      - name: Format
        run: cargo fmt --all

      - name: Phase 8 static proof
        run: python -W error tools/phase8_static_review.py

      - name: Workspace type proof
        run: cargo check --workspace --all-targets --locked

      - name: Clippy proof
        run: cargo clippy --workspace --all-targets --locked -- -D warnings

      - name: Runtime executor tests
        run: cargo test --locked -p neo-runtime-executor

      - name: Diff check
        shell: bash
        run: git diff --check

      - name: Self-clean and commit
        shell: bash
        run: |
          rm tools/phase8_linux_clippy_once.py .github/workflows/phase8-linux-clippy-once.yml
          git add -A
          git config user.name "THE TECH GUY"
          git config user.email "65334711+jaydumisuni@users.noreply.github.com"
          git commit -m "Phase 8: close Linux cfg hygiene [phase8-linux-clippy-done]"
          git push origin HEAD:implementation/phase-8-runtime-executor
