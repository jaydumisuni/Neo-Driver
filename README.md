# Neo Driver

Neo Driver is a planned model-free Windows setup, driver, runtime, gaming, technician, debloat, tweak, repair, and recovery suite.

> **Status:** Architecture / planning freeze. No implementation has started.

The canonical implementation plan is [`docs/NEO_DRIVER_MASTER_PLAN.md`](docs/NEO_DRIVER_MASTER_PLAN.md).

Implementation must not drift from the master plan silently. Any material architecture, scope, safety, UX, or execution-doctrine change must be explicitly approved and then recorded in the master plan before implementation continues.
